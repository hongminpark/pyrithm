def solution(a,b):
    for i in range(len(a)):
        x = a[i]
        if (x not in b):
            return x
        else :
            for j in range(i+1, len(a)):
                if a[j] == x:
			              return x
